namespace ConsoleApp1
{
    class SUV : Vehicle
    {
        public SUV(string model, string manufacturer, int weight, string color, int maxSpeed) : base(model, manufacturer, weight, color, maxSpeed)
        {
        }
    }
}